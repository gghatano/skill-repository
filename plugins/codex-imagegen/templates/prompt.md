<!--
  生成用プロンプトの「フォーマット」。骨子 1 エントリ → この形の prompts/<stem>.md 1 ファイル。
  prompt-build スキルが outline.md からこの形へ変換する。**大事なのはこの構造**であって、
  生成手段（手で書く／スクリプトで書き出す）は問わない。< > を骨子の値で置き換える。

  上半分（見出し「生成ルール」「文体」「組版」「出力」）は毎回ほぼ同じ固定部。
  下半分（label〜facts）が骨子ごとに変わる部。
-->

「画像生成ルール」（AGENTS.md）に従い、下を **1 枚の図解** に整理する。
**文章をカードに並べるだけにしない。** アイコン・配置・矢印で、見れば構造が分かる図にする。
数字・日本語ラベルは一字も変えない。

## 文体（図の中のすべての文言に適用）
＜outline の house-style をここに差し込む＞

## 組版（STYLE.md の下限を必ず満たす）
- **文字は大きく。** STYLE.md の文字サイズ下限を満たす（1920px 幅での実寸）。入らないなら要素を減らす。縮めない。
- **日本語を助詞や 1 文字で折らない。** 文節ごとに `display:inline-block` で包むか `<br>` を自分で置く。
- **絵文字・装飾記号・アイコンフォントを使わない**（`□` が出る）。図形は SVG か罫線で描く。
- **同じ文言を 2 箇所に出さない。**「最重要メッセージ」を図の中にそのまま貼らない。
- **線は 1 関係 1 本。** 端点はカードの縁に接続する。宙に浮いた線・二重の矢印を作らない。並列の列挙に矢印を使わない。

## ラベル（上に小さく添えるキッカー）
＜label＞

## ヘッドライン（完全な文のメッセージ＝主タイトル。最も大きく）
＜headline＞

## 最重要メッセージ（この 1 枚で理解してほしい 1 つ）
＜key＞

## 想定読者
＜audience＞

## 希望する構造
＜structure＞

## 特に強調したいこと（視覚的に）
＜emphasis＞

## 素材（図に落とす中身。※そのまま箇条書きで並べない。構造に符号化する）
### ＜見出し 1＞
- ＜要点＞
- ＜要点＞

### ＜見出し 2＞
- ＜要点＞
- ＜要点＞

## 必ず正確に載せる数値（変えない）
＜facts をカンマ区切りで。なければこの節ごと省く＞

## 出力
- infographic PNG: `out/<stem>.png`（フルスライド 16:9・幅 1920px・ライト地）。
- 見れば構造が伝わる図に。テキストは要点のキーワードと数値だけを図の部品に埋める。
